{
  "description": "Enforces conversation context in git commits",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/check-git-commit.js"
          }
        ]
      }
    ]
  }
}
